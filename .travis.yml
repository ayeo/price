language: php

env:
 - CODECOV_TOKEN="86760c0b-d0b1-4f5d-be3f-d7b4fac9c9c3"

php:
 - 5.4
 - 5.5
 - 5.6
 - 7.0
 - 7.1
 - 7.2
 - 7.3

sudo: false

install:
 - travis_retry composer install --no-interaction --prefer-source

script:
 - vendor/bin/phpunit --coverage-clover=coverage.xml --verbose

after_success:
 - bash <(curl -s https://codecov.io/bash)